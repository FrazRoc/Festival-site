//Filename: models/project/ProjectModel.js
define([
	'underscore',
	'backbone'
], function(_, Backbone){
	var ProjectModel = Backbone.Model.extend({
		defaults: {

		},

		initialize: function(){
		}
	});

	return ProjectModel;  
});

